--original docker-compose.yaml

services:
  pgdatabase:
    image: postgres:18
    environment:
      POSTGRES_USER: "root"
      POSTGRES_PASSWORD: "root"
      POSTGRES_DB: "ny_taxi"
    volumes:
      - "ny_taxi_postgres_data:/var/lib/postgresql/data"
    ports:
      - "5432:5432"

  pgadmin:
    image: dpage/pgadmin4
    environment:
      PGADMIN_DEFAULT_EMAIL: "admin@admin.com"
      PGADMIN_DEFAULT_PASSWORD: "root"
    volumes:
      - "pgadmin_data:/var/lib/pgadmin"
    ports:
      - "8085:80"


volumes:
  ny_taxi_postgres_data:
  pgadmin_data:



--Run commands
--postgres database container
docker run -it --rm \
  -e POSTGRES_USER="root" \
  -e POSTGRES_PASSWORD="root" \
  -e POSTGRES_DB="ny_taxi" \
  -v ny_taxi_postgres_data:/var/lib/postgresql \
  -p 5432:5432 \
  --network=pg-network \
  --name pgdatabase \
  postgres:18

--pgadmin container
docker run -it \
  -e PGADMIN_DEFAULT_EMAIL="admin@admin.com" \
  -e PGADMIN_DEFAULT_PASSWORD="root" \
  -v pgadmin_data:/var/lib/pgadmin \
  -p 8085:80 \
  --network=pg-network \
  --name pgadmin \
  dpage/pgadmin4

--running them on the same network
docker run -it --rm \
  --network=pipeline_default \
  taxi_ingest:v001 \
    --pg-user=root \
    --pg-password=root \
    --pg-host=pgdatabase \
    --pg-port=5432 \
    --pg-database=ny_taxi \
    --target-table=yellow_taxi_trips_2021_1


--Run commands for modified docker-compose.yaml file
docker compose run --rm ingest ingest_data_cli.py \
  --pg-user root \
  --pg-password root \
  --pg-host pgdatabase \
  --pg-port 5432 \
  --pg-database ny_taxi \
  --target-table yellow_taxi_trips_2021_1 \
  --year 2021 \
  --month 1


docker compose run --rm ingest ingest_lookup_cli.py \
  --pg-user root \
  --pg-password root \
  --pg-host pgdatabase \
  --pg-port 5432 \
  --pg-database ny_taxi \
  --target-table yellow_taxi_location_lookup \
  --csv-path /data/taxi_zone_lookup.csv